import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    'timingContainer': {
        marginTop: 10,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center'
    }
});